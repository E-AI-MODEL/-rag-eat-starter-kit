# Compressed data

This directory is the repository's gzip boundary.

Only valid `.gz` data files belong below this point. Keep this README uncompressed so the rule remains visible in GitHub.

Use `rag-eat-gzip check` to validate the directory and read `docs/GZIP_STORAGE.md` for the full workflow.
