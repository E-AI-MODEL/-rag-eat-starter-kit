# Evaluation

Use `rag_eval_set.csv` as a starting point.

Replace the synthetic examples with safe test questions.

Check source recall, answer quality, citation quality, missing context and access rules.
